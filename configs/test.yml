input:
  address: datasets/test.csv
  format: csv
  missing_values:
    - none
  headers: true

parsing:
  - ordinal
  - ordinal
  - numeric

scrub:
  - name: mean
    index: 2

transform:
  - name: zscore
    index: 2

model:
  name: null-classifier
  label_index: 2
  validation: 
    strategy: stratified-kfold
    parameters:
      num_folds: 2
  evaluation: mse
